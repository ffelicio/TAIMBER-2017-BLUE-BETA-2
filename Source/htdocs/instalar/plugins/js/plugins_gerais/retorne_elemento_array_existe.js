
// retorna se o elemento do array existe
function retorne_elemento_array_existe(array_elementos, elemento){

// valida index de elemento em array
if(array_elementos.indexOf(elemento) > -1){
	
	// elemento existe
	return true;
	
}else{
	
	// elemento nao existe
	return false;
	
};

};
