
// retorna se a variavel existe
function retorne_variavel_existe(v_variavel){

// valida se variavel existe
if(typeof v_variavel === 'undefined'){
  
    // nao existe
    return false;

}else{
	
	// existe
	return true;
	
};

};
